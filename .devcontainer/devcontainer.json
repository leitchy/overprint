{
  "name": "Overprint Dev Container",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "postCreateCommand": "npm cache clean --force && npm install -g @anthropic-ai/claude-code",
  "secrets": {
    "ANTHROPIC_API_KEY": {
      "description": "API key for Claude Code"
    }
  }
}